CREATE TABLE users (
    idUsers int AUTO_INCREMENT NOT NULL,
    uidUsers varchar(50) NOT NULL,
    emailUsers text NOT NULL,
    pwdUsers text NOT NULL,
    phoneUsers varchar(10) NOT NULL,
    PRIMARY KEY(idUsers)
);